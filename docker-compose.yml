services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      LISTEN_HOST: "0.0.0.0"
      PORT: "8000"
    volumes:
      - app-data:/app/backend/data/generated
    restart: unless-stopped

volumes:
  app-data:
